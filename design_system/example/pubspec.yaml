name: example
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  orange_design_system:
    path: ../
  orange_design_box:
    path: ../../design_box

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0